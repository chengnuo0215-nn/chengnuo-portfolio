# 程诺个人网站设计记录

## Visual World

整体为深色作品档案台：接近纯黑的背景、细颗粒纹理、低透明度石墨面板和首屏缓慢流动的黑绿 Grainient。页面避免模板化的居中 hero 和等分信息卡，以姓名、角色、学历和实习经历建立第一印象。

## Type And Layout

字体使用系统无衬线，中文优先 `PingFang SC`，英文主标题使用自托管 Montserrat。主要布局为求职档案流：首屏身份直接进入标题行，写作头像 + `Hi，我是` + `程诺`；顶部导航在首屏关键词滑出页面上方后才出现，作为独立的居中纯 icon 胶囊导航，不展示头像和姓名。首屏优先展示关键词、学历、联系方式和实习经历，`UX Designer` 进入关键词轮播并使用较轻的英文样式。学历与联系方式组成左列，实习经历为右列；项目经验紧随其后；更多关于我和思考记录后置。圆角收敛为 12px 小圆角，项目封面统一 16:9，并把项目部门、标题、类型融合到封面图层中。

## Components

- `FloatingNav`：滚动后出现的顶部居中胶囊导航。
- `Hero`：中文身份标题、头像、关键词轮播、学历、联系方式和实习经历。
- `MoreAbout`：联系方式、能力标签和个人状态。
- `Projects`：项目卡片保留详情页链接接口，桌面端统一两列等规格；每张卡片包含类别标签、项目标题、一句特点介绍、16:9 封面和底部“查看项目”入口。
- `Notes`：设计心得和记录入口。

## Motion

动效只用于背景环境光、关键词切换和必要 hover 反馈，避免过度炫技。背景采用 Grainient WebGL shader 慢速流动，调成深蓝和黑色的光影变化；它在页面隐藏时暂停，在减少动态偏好下只保留静态渲染。
